import { createHash } from 'node:crypto';
import { readFile, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const OUT_DIR = resolve(process.cwd(), 'public/missions/mission01');
const configs = {
  6: {
    title: 'MISIÓN CIENTÍFICA',
    subtitle: 'Usa un bucle para llegar al punto de estudio y completa el ciclo científico.',
    accent: '#49C9D7',
    start: { c: 1, r: 5, dir: 0 },
    goal: { c: 5, r: 5 },
    scienceCells: [{ c: 5, r: 5, label: 'PUNTO DE ESTUDIO' }],
    blocks: ['forward', 'left', 'right', 'repeat', 'scan', 'analyze', 'send'],
    explore: [
      ['RUTA CIENTÍFICA', 'AYNI debe llegar al punto iluminado sin desperdiciar instrucciones.', 'Mira cuántos AVANZAR se repiten.'],
      ['BUCLE', 'REPETIR × N ejecuta varias veces los bloques que coloques dentro.', 'Puedes colocar AVANZAR dentro del bucle.'],
      ['CICLO DE CIENCIA', 'En el punto de estudio debes ESCANEAR, luego ANALIZAR y finalmente ENVIAR DATOS.', 'El orden importa porque cada acción usa el resultado anterior.'],
      ['OBJETIVO', 'Combina movimiento y ciencia en un solo programa compacto.', 'EXPLORAR y GUÍA son ayudas opcionales.'],
    ],
    guideTasks: ['1 · LLEGA CON REPETIR', '2 · ESCANEA Y ANALIZA', '3 · ENVÍA LOS DATOS'],
    introText: 'Programa a AYNI. La ruta recta tiene un patrón claro: aprovéchalo con REPETIR.',
    successText: 'Usaste un bucle para desplazarte y completaste el ciclo ESCANEAR → ANALIZAR → ENVIAR DATOS.',
    journalTitle: 'NIVEL 6 · MISIÓN CIENTÍFICA',
    journalText: 'Un bucle puede compactar movimiento repetido y dejar el programa más claro para las acciones científicas.',
  },
  7: {
    title: 'SENSORES Y BUCLES',
    subtitle: 'Repite una secuencia de movimiento y lectura para registrar dos sensores.',
    accent: '#A8EDF1',
    start: { c: 1, r: 5, dir: 0 },
    goal: { c: 6, r: 5 },
    sensorCells: [
      { c: 3, r: 5, label: 'SENSOR 1', value: '18 °C' },
      { c: 5, r: 5, label: 'SENSOR 2', value: '23 °C' },
    ],
    blocks: ['forward', 'left', 'right', 'repeat', 'read', 'record', 'send'],
    explore: [
      ['DOS SENSORES', 'Hay dos puntos cyan sobre la ruta. AYNI debe registrar ambos.', 'Observa que la misma secuencia sirve para los dos.'],
      ['PATRÓN', 'Entre un sensor y el siguiente se repiten movimiento, lectura y registro.', 'Un bucle puede contener más de una instrucción.'],
      ['DATOS', 'LEER SENSOR obtiene el valor actual y REGISTRAR DATO lo guarda en la bitácora.', 'No puedes registrar antes de leer.'],
      ['CIERRE', 'Después de registrar ambos sensores, llega a la bandera y ENVÍA DATOS.', 'EXPLORAR y GUÍA siguen siendo opcionales.'],
    ],
    guideTasks: ['1 · REGISTRA SENSOR 1', '2 · REGISTRA SENSOR 2', '3 · LLEGA Y ENVÍA DATOS'],
    introText: 'Haz que una misma secuencia trabaje dos veces: muévete, lee y registra cada sensor.',
    successText: 'Repetiste una secuencia con sensores y enviaste dos registros completos.',
    journalTitle: 'NIVEL 7 · SENSORES Y BUCLES',
    journalText: 'REPETIR también sirve cuando un patrón combina movimiento y lectura de sensores.',
  },
};

const esc = (value) => String(value).replace(/[&<>"']/g, (ch) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const hash = (text) => createHash('sha256').update(Buffer.from(text, 'utf8')).digest('hex');

function levelHtml(level, cfg) {
  const config = JSON.stringify(cfg).replaceAll('</', '<\\/');
  return `<!doctype html>
<html lang="es" data-apulab-level="${level}">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>AYNI · Misión 01 · Nivel ${level} — ${esc(cfg.title)}</title>
<style>
:root{--bg:#0B0E26;--panel:#141938;--purple:#2D2654;--purple2:#3B326B;--border:#4D4288;--lav:#8E7DCE;--cyan:#49C9D7;--cyan2:#A8EDF1;--yellow:#F4C75E;--cream:#FFF7E8;--white:#fff;--muted:#B8C2CC;--dark:#17133A;--pink:#F3A6C8}
*{box-sizing:border-box}html,body{margin:0;width:100%;height:100%;overflow:hidden;background:var(--bg);font-family:Poppins,Arial,sans-serif;color:var(--white)}button{font:inherit}.stage{position:relative;width:100%;height:100%;min-height:720px;background:radial-gradient(circle at 72% 28%,rgba(73,201,215,.08),transparent 28%),linear-gradient(180deg,#0B0E26,#111536)}
.header{height:88px;display:flex;align-items:center;padding:14px 22px;gap:14px;border-bottom:2px solid var(--border);background:rgba(20,25,56,.98);position:relative;z-index:20}.level-badge{padding:8px 11px;background:var(--yellow);color:var(--dark);border:2px solid var(--dark);box-shadow:4px 4px 0 #D5A43D;font-weight:900}.title-wrap{min-width:340px}.title{font-size:22px;font-weight:900;letter-spacing:.03em}.subtitle{font-size:12px;color:var(--muted);margin-top:3px}.hud{margin-left:auto;display:flex;gap:9px;align-items:center}.btn{border:2px solid var(--dark);border-radius:5px;padding:10px 13px;font-weight:900;letter-spacing:.02em;cursor:pointer;box-shadow:4px 4px 0 var(--dark)}.btn:active{transform:translate(2px,2px);box-shadow:2px 2px 0 var(--dark)}.btn-yellow{background:var(--yellow);color:var(--dark)}.btn-purple{background:var(--lav);color:var(--dark)}.btn-cyan{background:var(--cyan);color:var(--dark)}.btn-progress{background:var(--purple2);color:#fff;cursor:default}.main{height:calc(100% - 88px);display:grid;grid-template-columns:235px minmax(520px,1fr) 360px;gap:12px;padding:12px}.panel{background:rgba(20,25,56,.96);border:2px solid var(--border);border-radius:8px;box-shadow:6px 6px 0 rgba(23,19,58,.75)}
.palette{padding:14px;overflow:auto}.palette h3,.workspace h3{margin:0 0 10px;font-size:13px;letter-spacing:.12em;color:var(--cyan2)}.help{font-size:12px;line-height:1.4;color:var(--muted);margin-bottom:12px}.group-label{font-size:10px;color:var(--muted);letter-spacing:.14em;font-weight:900;margin:12px 0 6px}.command{display:flex;align-items:center;gap:9px;width:100%;border:2px solid var(--dark);border-radius:5px;padding:10px;margin:7px 0;background:var(--cream);color:var(--dark);font-weight:900;cursor:pointer;box-shadow:3px 3px 0 var(--dark);text-align:left}.command[data-command="repeat"]{background:#C9F6F7}.command[data-command="scan"],.command[data-command="read"]{background:#B9E7FF}.command[data-command="analyze"],.command[data-command="record"]{background:#E3D6FF}.command[data-command="send"]{background:#FFD7A6}.command .ico{width:24px;text-align:center}.command:hover{transform:translateY(-1px)}
.board-wrap{position:relative;overflow:hidden;min-width:0}.board-wrap canvas{display:block;width:100%;height:100%}.board-label{position:absolute;left:14px;top:14px;z-index:4;padding:7px 9px;background:rgba(20,25,56,.9);border:1px solid var(--border);font-size:11px;color:var(--cyan2);font-weight:800}.status{position:absolute;left:50%;bottom:14px;transform:translateX(-50%);z-index:5;max-width:80%;padding:9px 12px;border:2px solid var(--dark);background:var(--purple2);box-shadow:4px 4px 0 var(--dark);border-radius:5px;font-size:12px;font-weight:800;text-align:center;opacity:0;transition:opacity .16s}.status.show{opacity:1}
.workspace{padding:14px;display:flex;flex-direction:column;min-height:0}.workspace-top{display:flex;align-items:center;justify-content:space-between;gap:8px}.nest-note{font-size:10px;color:var(--yellow);font-weight:800}.program{flex:1;min-height:220px;overflow:auto;background:#0F1330;border:2px dashed var(--border);border-radius:6px;padding:9px}.empty{color:#73809a;font-size:12px;text-align:center;padding:36px 8px}.program-node{position:relative;margin:7px 0;padding:9px 10px;border:2px solid var(--dark);border-radius:5px;background:var(--cream);color:var(--dark);font-size:12px;font-weight:900;box-shadow:3px 3px 0 var(--dark)}.program-node.repeat{background:#C9F6F7}.program-node.active{outline:3px solid var(--yellow)}.repeat-head{display:flex;align-items:center;gap:7px}.repeat-count{margin-left:auto;display:flex;align-items:center;gap:5px}.mini{width:25px;height:25px;border:2px solid var(--dark);background:#fff;font-weight:900;cursor:pointer}.repeat-body{margin-top:8px;padding:6px 8px 6px 18px;border-left:4px solid var(--cyan);background:rgba(255,255,255,.5);min-height:34px}.node-remove{position:absolute;right:4px;top:4px;width:20px;height:20px;border:0;background:transparent;font-weight:900;cursor:pointer}.actions{display:flex;gap:8px;margin-top:10px}.actions .btn{flex:1}.feedback{min-height:54px;margin-top:9px;padding:9px;border:1px solid var(--border);background:#101530;color:var(--muted);font-size:12px;line-height:1.35}.sensor-readouts{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}.readout{padding:5px 7px;border:1px solid var(--cyan);background:rgba(73,201,215,.1);font-size:10px;color:var(--cyan2);font-weight:800}
.info-panel{position:absolute;left:260px;top:106px;z-index:30;width:330px;min-height:210px;padding:15px 16px 14px;border:2px solid var(--dark);border-radius:6px;background:var(--purple2);box-shadow:7px 7px 0 var(--dark);display:none}.info-panel.visible{display:block}.info-panel.apulab-explore-yellow{background:#F4C75E;color:var(--dark)}.info-panel small{display:block;font-size:10px;letter-spacing:.13em;font-weight:900;color:var(--cyan2)}.info-panel.apulab-explore-yellow small{color:var(--dark)}.info-panel strong{display:block;margin:5px 0 8px;font-size:18px}.info-panel p{font-size:12px;line-height:1.45;margin:7px 0}.info-close{position:absolute;right:7px;top:7px;border:0;background:transparent;color:inherit;font-size:22px;cursor:pointer}.info-progress{position:absolute;right:38px;top:11px;font-size:11px;font-weight:900}.guide-list{display:flex;flex-direction:column;gap:8px;margin-top:6px}.guide-task{position:relative;display:block;padding:7px 9px;border-radius:4px;background:rgba(11,14,38,.28);color:#fff;font-weight:900}.guide-task.active{outline:2px solid var(--cyan2)}.guide-task.completed{opacity:.72}.guide-task.completed::after{content:"";position:absolute;left:7px;right:7px;top:50%;height:3px;background:var(--pink);transform:rotate(-2deg);border-radius:3px}.hint{color:var(--cyan2)}
.overlay{position:absolute;inset:0;z-index:60;display:none;align-items:center;justify-content:center;background:rgba(7,9,27,.82)}.overlay.visible{display:flex}.popup{width:min(560px,90vw);padding:22px;border:2px solid var(--border);border-radius:8px;background:var(--panel);box-shadow:10px 10px 0 var(--dark);position:relative}.popup h2{margin:0 0 8px;color:var(--yellow)}.popup p{color:var(--muted);line-height:1.45}.popup-close{position:absolute;right:8px;top:8px;border:0;background:transparent;color:#fff;font-size:24px;cursor:pointer}.journal-card{padding:12px;border:1px solid var(--cyan);background:rgba(73,201,215,.08);margin:12px 0}.journal-card strong{display:block;color:var(--cyan2);margin-bottom:5px}.popup-actions{display:flex;gap:9px;justify-content:flex-end;margin-top:14px}.confetti{position:absolute;inset:0;z-index:90;pointer-events:none;overflow:hidden}.piece{position:absolute;top:-20px;width:8px;height:14px;background:var(--yellow);animation:fall 2.5s linear forwards}@keyframes fall{to{transform:translate3d(var(--dx),110vh,0) rotate(760deg);opacity:.7}}
@media(max-width:1100px){.main{grid-template-columns:210px 1fr 320px}.title-wrap{min-width:0}.subtitle{display:none}.info-panel{left:220px}}
</style>
</head>
<body>
<div class="stage" data-mission-level="${level}">
<header class="header"><div class="level-badge">NIVEL ${level}</div><div class="title-wrap"><div class="title">${esc(cfg.title)}</div><div class="subtitle">${esc(cfg.subtitle)}</div></div><nav class="hud"><button id="explore-btn" class="btn btn-yellow">▶ EXPLORAR</button><button id="guide-btn" class="btn btn-purple">▶ GUÍA</button><button id="journal-btn" class="btn btn-cyan">▶ BITÁCORA</button><div class="btn btn-progress">${level} / 7</div></nav></header>
<main class="main">
<aside class="panel palette"><h3>COMANDOS</h3><div class="help">Haz clic en un bloque para añadirlo. Cuando un bloque REPETIR esté seleccionado, los nuevos comandos se insertan dentro.</div><div class="group-label">MOVIMIENTO</div><button class="command" data-command="forward"><span class="ico">↑</span>AVANZAR</button><button class="command" data-command="left"><span class="ico">↶</span>GIRAR IZQ.</button><button class="command" data-command="right"><span class="ico">↷</span>GIRAR DER.</button><div class="group-label">CONTROL</div><button class="command" data-command="repeat"><span class="ico">↻</span>REPETIR × N</button><div class="group-label">${level===6?'CIENCIA':'SENSORES'}</div>${level===6?'<button class="command" data-command="scan"><span class="ico">⌁</span>ESCANEAR</button><button class="command" data-command="analyze"><span class="ico">◇</span>ANALIZAR</button>':'<button class="command" data-command="read"><span class="ico">⌁</span>LEER SENSOR</button><button class="command" data-command="record"><span class="ico">▣</span>REGISTRAR DATO</button>'}<button class="command" data-command="send"><span class="ico">⇧</span>ENVIAR DATOS</button></aside>
<section class="panel board-wrap"><div class="board-label">AYNI · FRENTE = LUZ CYAN</div><canvas id="board-canvas" width="950" height="664"></canvas><div id="status" class="status"></div></section>
<section class="panel workspace"><div class="workspace-top"><h3>MI PROGRAMA</h3><span id="nest-note" class="nest-note">NIVEL PRINCIPAL</span></div><div id="program" class="program"></div><div class="actions"><button id="exit-repeat" class="btn btn-cyan" hidden>SALIR DEL BUCLE</button><button id="clear-btn" class="btn btn-purple">LIMPIAR</button><button id="run-btn" class="btn btn-yellow">▶ EJECUTAR</button></div><div id="feedback" class="feedback">${esc(cfg.introText)}</div><div id="sensor-readouts" class="sensor-readouts"></div></section>
</main>
<section id="info-panel" class="info-panel"><button id="info-close" class="info-close">×</button><span id="info-progress" class="info-progress"></span><small id="info-kicker">EXPLORAR</small><strong id="info-title"></strong><p id="info-text"></p><p id="info-hint" class="hint"></p></section>
<div id="journal-overlay" class="overlay"><section class="popup"><button id="journal-close" class="popup-close">×</button><h2>BITÁCORA</h2><div class="journal-card"><strong>${esc(cfg.journalTitle)}</strong><p>${esc(cfg.journalText)}</p><div id="journal-data"></div></div></section></div>
<div id="success-overlay" class="overlay"><section class="popup"><h2>¡NIVEL ${level} COMPLETADO!</h2><p>${esc(cfg.successText)}</p><div class="journal-card"><strong>BITÁCORA ACTUALIZADA</strong><p id="success-data"></p></div><div class="popup-actions">${level===6?'<button id="continue-btn" class="btn btn-yellow">CONTINUAR AL NIVEL 7</button>':'<button id="finish-btn" class="btn btn-yellow">MISIÓN 01 COMPLETADA</button>'}</div></section></div><div id="confetti" class="confetti"></div>
</div>
<script type="module">
const LEVEL=${level};const CFG=${config};
const nativeRAF=window.requestAnimationFrame.bind(window);const nativeCAF=window.cancelAnimationFrame.bind(window);const rafIds=new Set();let disposed=false;window.requestAnimationFrame=(cb)=>{if(disposed)return 0;let id=0;id=nativeRAF((t)=>{rafIds.delete(id);if(!disposed)cb(t)});rafIds.add(id);return id};window.cancelAnimationFrame=(id)=>{rafIds.delete(id);nativeCAF(id)};
let THREE;try{THREE=await import('https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js')}catch{THREE=await import('https://esm.sh/three@0.180.0')}
const canvas=document.getElementById('board-canvas');const LOW=((navigator.hardwareConcurrency||8)<=4)||((navigator.deviceMemory||8)<=4);const renderer=new THREE.WebGLRenderer({canvas,antialias:!LOW,alpha:true,powerPreference:'high-performance'});renderer.setPixelRatio(LOW?1:Math.min(devicePixelRatio||1,1.2));renderer.setSize(950,664,false);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.05;const scene=new THREE.Scene();scene.background=new THREE.Color(0x1f203f);scene.fog=new THREE.Fog(0x1f203f,12,22);const camera=new THREE.PerspectiveCamera(35,950/664,.1,100);camera.position.set(0,10.4,8.7);camera.lookAt(0,0,0);scene.add(new THREE.HemisphereLight(0xD7F7FF,0x2D173A,2.2));const key=new THREE.DirectionalLight(0xFFF3D6,3);key.position.set(-5,9,5);scene.add(key);const rim=new THREE.DirectionalLight(0x49C9D7,1.3);rim.position.set(6,5,-5);scene.add(rim);
const mat=(c,e=0)=>new THREE.MeshStandardMaterial({color:c,roughness:.72,metalness:.05,emissive:e,emissiveIntensity:e?0.5:0});const ground=new THREE.Mesh(new THREE.PlaneGeometry(18,14),mat(0x3E283B));ground.rotation.x=-Math.PI/2;ground.position.y=-.35;scene.add(ground);const board=new THREE.Mesh(new THREE.BoxGeometry(8.4,.32,7.6),mat(0x2D2654));board.position.y=-.14;scene.add(board);
const tiles=[];const size=.82,gap=.11;const cellPos=(c,r)=>new THREE.Vector3((c-3.5)*(size+gap),.15,(r-3.5)*(size+gap));for(let r=0;r<8;r++){tiles[r]=[];for(let c=0;c<8;c++){const tile=new THREE.Mesh(new THREE.BoxGeometry(size,.16,size),mat((r+c)%2?0x714B66:0x80536D));tile.position.copy(cellPos(c,r));tile.position.y=.08;scene.add(tile);tiles[r][c]=tile}}
const markerMat=mat(0x49C9D7,0x49C9D7);const markers=[];for(const cell of (CFG.scienceCells||CFG.sensorCells||[])){const ring=new THREE.Mesh(new THREE.TorusGeometry(.28,.045,10,32),markerMat);ring.rotation.x=Math.PI/2;ring.position.copy(cellPos(cell.c,cell.r));ring.position.y=.28;scene.add(ring);markers.push(ring)}const goalRing=new THREE.Mesh(new THREE.TorusGeometry(.34,.055,10,32),mat(0xF4C75E,0xF4C75E));goalRing.rotation.x=Math.PI/2;goalRing.position.copy(cellPos(CFG.goal.c,CFG.goal.r));goalRing.position.y=.3;scene.add(goalRing);
function createRover(){const g=new THREE.Group();const body=new THREE.Mesh(new THREE.BoxGeometry(.56,.24,.72),mat(0xEEE8DF));body.position.y=.32;g.add(body);const nose=new THREE.Mesh(new THREE.BoxGeometry(.42,.12,.12),mat(0x49C9D7,0x49C9D7));nose.position.set(0,.35,-.39);g.add(nose);const eyeMat=mat(0x0B0E26);for(const x of [-.13,.13]){const eye=new THREE.Mesh(new THREE.SphereGeometry(.045,10,8),eyeMat);eye.position.set(x,.41,-.46);g.add(eye)}for(const x of [-.32,.32])for(const z of [-.24,.24]){const w=new THREE.Mesh(new THREE.CylinderGeometry(.09,.09,.12,12),mat(0x17133A));w.rotation.z=Math.PI/2;w.position.set(x,.19,z);g.add(w)}return g}const rover=createRover();scene.add(rover);
let roverState={...CFG.start};function syncRover(){rover.position.copy(cellPos(roverState.c,roverState.r));rover.position.y=.22;rover.rotation.y=[-Math.PI/2,0,Math.PI/2,Math.PI][roverState.dir]||0}syncRover();
let last=0;function render(t=0){requestAnimationFrame(render);const dt=Math.min(.05,(t-last)/1000||0);last=t;markers.forEach((m,i)=>{m.rotation.z+=dt*(.6+i*.08);m.material.emissiveIntensity=.45+Math.sin(t*.004+i)*.16});goalRing.rotation.z-=dt*.55;renderer.render(scene,camera)}requestAnimationFrame(render);
const labels={forward:'AVANZAR',left:'GIRAR IZQ.',right:'GIRAR DER.',repeat:'REPETIR',scan:'ESCANEAR',analyze:'ANALIZAR',read:'LEER SENSOR',record:'REGISTRAR DATO',send:'ENVIAR DATOS'};let program=[];let activeRepeat=null;let executing=false;let moved=false,scanned=false,analyzed=false,sent=false,lastReading=null;const records=[];const sleep=(ms)=>new Promise(r=>setTimeout(r,ms));const feedback=document.getElementById('feedback');const programEl=document.getElementById('program');const nestNote=document.getElementById('nest-note');const exitRepeat=document.getElementById('exit-repeat');const readouts=document.getElementById('sensor-readouts');
function target(){return activeRepeat?activeRepeat.body:program}function usesRepeat(nodes=program){return nodes.some(n=>n.type==='repeat'||(n.body&&usesRepeat(n.body)))}function nodeCount(nodes=program){return nodes.reduce((n,x)=>n+1+(x.body?nodeCount(x.body):0),0)}function addCommand(cmd){if(executing)return;if(cmd==='repeat'){const node={type:'repeat',count:2,body:[]};target().push(node);activeRepeat=node}else target().push({type:'cmd',cmd});renderProgram()}document.querySelectorAll('.command').forEach(b=>b.addEventListener('click',()=>addCommand(b.dataset.command)));
function renderProgram(){programEl.innerHTML='';if(!program.length){programEl.innerHTML='<div class="empty">Tu programa aparecerá aquí.</div>'}const draw=(nodes,parent)=>nodes.forEach((node,index)=>{const d=document.createElement('div');d.className='program-node'+(node.type==='repeat'?' repeat':'')+(node===activeRepeat?' active':'');if(node.type==='repeat'){d.innerHTML='<button class="node-remove">×</button><div class="repeat-head"><span>↻ REPETIR</span><span class="repeat-count"><button class="mini dec">−</button><b>× '+node.count+'</b><button class="mini inc">+</button></span></div><div class="repeat-body"></div>';d.querySelector('.dec').onclick=e=>{e.stopPropagation();node.count=Math.max(2,node.count-1);renderProgram()};d.querySelector('.inc').onclick=e=>{e.stopPropagation();node.count=Math.min(6,node.count+1);renderProgram()};d.onclick=()=>{activeRepeat=node;renderProgram()};draw(node.body,d.querySelector('.repeat-body'))}else d.innerHTML='<button class="node-remove">×</button>'+labels[node.cmd];d.querySelector('.node-remove').onclick=e=>{e.stopPropagation();nodes.splice(index,1);if(node===activeRepeat)activeRepeat=null;renderProgram()};parent.appendChild(d)});draw(program,programEl);nestNote.textContent=activeRepeat?'DENTRO DE REPETIR':'NIVEL PRINCIPAL';exitRepeat.hidden=!activeRepeat}exitRepeat.onclick=()=>{activeRepeat=null;renderProgram()};renderProgram();
function resetRun(){roverState={...CFG.start};syncRover();moved=false;scanned=false;analyzed=false;sent=false;lastReading=null;records.length=0;readouts.innerHTML='';updateGuideIfOpen()}
async function moveForward(){const dirs=[[1,0],[0,1],[-1,0],[0,-1]],d=dirs[roverState.dir],nc=roverState.c+d[0],nr=roverState.r+d[1];if(nc<0||nc>7||nr<0||nr>7)throw new Error('AYNI saldría del tablero. Corrige el programa.');roverState.c=nc;roverState.r=nr;moved=true;const start=rover.position.clone(),end=cellPos(nc,nr);end.y=.22;for(let i=1;i<=12;i++){rover.position.lerpVectors(start,end,i/12);await sleep(24)}}async function turn(delta){roverState.dir=(roverState.dir+delta+4)%4;const end=[-Math.PI/2,0,Math.PI/2,Math.PI][roverState.dir]||0,start=rover.rotation.y;for(let i=1;i<=10;i++){rover.rotation.y=start+(end-start)*i/10;await sleep(22)}}
function scienceHere(){return (CFG.scienceCells||[]).some(x=>x.c===roverState.c&&x.r===roverState.r)}function sensorHere(){return (CFG.sensorCells||[]).find(x=>x.c===roverState.c&&x.r===roverState.r)}async function execCmd(cmd){if(cmd==='forward')return moveForward();if(cmd==='left')return turn(-1);if(cmd==='right')return turn(1);if(cmd==='scan'){if(!scienceHere())throw new Error('ESCANEAR solo funciona en el punto de estudio cyan.');scanned=true;showStatus('Muestra escaneada.');await sleep(320);return}if(cmd==='analyze'){if(!scanned)throw new Error('Primero ESCANEA la muestra.');analyzed=true;showStatus('Análisis listo.');await sleep(320);return}if(cmd==='read'){const s=sensorHere();if(!s)throw new Error('LEER SENSOR solo funciona sobre un punto cyan.');lastReading=s;showStatus(s.label+' · '+s.value);await sleep(320);return}if(cmd==='record'){if(!lastReading)throw new Error('Primero LEER SENSOR.');if(!records.some(x=>x.label===lastReading.label)){records.push(lastReading);renderReadouts()}lastReading=null;await sleep(280);return}if(cmd==='send'){if(LEVEL===6&&!analyzed)throw new Error('Antes de ENVIAR DATOS debes ESCANEAR y ANALIZAR.');if(LEVEL===7&&records.length<2)throw new Error('Registra los dos sensores antes de ENVIAR DATOS.');sent=true;showStatus('Datos enviados a ApuLab Station.');await sleep(350);return}}
async function execute(nodes){for(const node of nodes){if(node.type==='repeat'){if(!node.body.length)throw new Error('REPETIR está vacío. Coloca bloques dentro.');for(let i=0;i<node.count;i++){showStatus('REPETIR · VUELTA '+(i+1)+' / '+node.count);await execute(node.body)}}else await execCmd(node.cmd);updateGuideIfOpen()}}
async function runProgram(){if(executing||!program.length)return;executing=true;activeRepeat=null;renderProgram();resetRun();feedback.textContent='AYNI está ejecutando tu programa…';try{await execute(program);const ok=LEVEL===6?roverState.c===CFG.goal.c&&roverState.r===CFG.goal.r&&sent&&usesRepeat():roverState.c===CFG.goal.c&&roverState.r===CFG.goal.r&&sent&&records.length===2&&usesRepeat();if(!ok){if(!usesRepeat())feedback.textContent='La ruta puede funcionar, pero este nivel pide usar REPETIR.';else if(roverState.c!==CFG.goal.c||roverState.r!==CFG.goal.r)feedback.textContent='AYNI terminó lejos de la meta amarilla. Ajusta el movimiento.';else feedback.textContent=LEVEL===6?'Completa ESCANEAR → ANALIZAR → ENVIAR DATOS.':'Registra ambos sensores y luego ENVÍA DATOS.'}else completeLevel()}catch(e){feedback.textContent=e.message||String(e)}finally{executing=false;updateGuideIfOpen()}}document.getElementById('run-btn').onclick=runProgram;document.getElementById('clear-btn').onclick=()=>{if(executing)return;program=[];activeRepeat=null;resetRun();renderProgram();feedback.textContent=CFG.introText};
function renderReadouts(){readouts.innerHTML=records.map(x=>'<span class="readout">'+x.label+' · '+x.value+'</span>').join('')}
function showStatus(text,ms=1200){const s=document.getElementById('status');s.textContent=text;s.classList.add('show');clearTimeout(showStatus.t);showStatus.t=setTimeout(()=>s.classList.remove('show'),ms)}
let exploreIndex=-1,guideOpen=false;const info=document.getElementById('info-panel');const progress=document.getElementById('info-progress');document.getElementById('explore-btn').onclick=()=>{guideOpen=false;exploreIndex=(exploreIndex+1)%CFG.explore.length;const s=CFG.explore[exploreIndex];info.className='info-panel visible apulab-explore-yellow';document.getElementById('info-kicker').textContent='EXPLORAR';document.getElementById('info-title').textContent=s[0];document.getElementById('info-text').textContent=s[1];document.getElementById('info-hint').textContent=s[2];progress.textContent=(exploreIndex+1)+' / '+CFG.explore.length};function guideActiveIndex(){if(LEVEL===6){if(sent)return 3;if(scanned&&analyzed)return 2;if(roverState.c===CFG.goal.c&&roverState.r===CFG.goal.r)return 1;return 0}if(sent)return 3;if(records.length>=2)return 2;if(records.length>=1)return 1;return 0}function renderGuide(){guideOpen=true;const active=guideActiveIndex();info.className='info-panel visible';document.getElementById('info-kicker').textContent='GUÍA';document.getElementById('info-title').textContent='GUÍA · 3 PASOS';document.getElementById('info-text').innerHTML='<span class="guide-list">'+CFG.guideTasks.map((x,i)=>'<span class="guide-task '+(i<active?'completed':i===active&&active<3?'active':'pending')+'">'+x+'</span>').join('')+'</span>';document.getElementById('info-hint').textContent=LEVEL===6?'Pista: REPETIR × 4 con AVANZAR es una forma compacta de llegar al punto.':'Pista: REPETIR × 2 puede contener AVANZAR, AVANZAR, LEER SENSOR y REGISTRAR DATO.';progress.textContent=''}document.getElementById('guide-btn').onclick=renderGuide;function updateGuideIfOpen(){if(guideOpen)renderGuide()}document.getElementById('info-close').onclick=()=>{info.className='info-panel';exploreIndex=-1;guideOpen=false};
function journalHtml(){if(LEVEL===6)return '<p>Estado científico: '+(scanned?'ESCANEADO':'pendiente')+' · '+(analyzed?'ANALIZADO':'pendiente')+' · '+(sent?'ENVIADO':'pendiente')+'</p>';return '<p>'+((records.length?records.map(x=>x.label+' = '+x.value).join('<br>'):'Aún no hay registros.'))+'</p>'}function openJournal(){document.getElementById('journal-data').innerHTML=journalHtml();document.getElementById('journal-overlay').classList.add('visible')}document.getElementById('journal-btn').onclick=openJournal;document.getElementById('journal-close').onclick=()=>document.getElementById('journal-overlay').classList.remove('visible');
function confetti(){const box=document.getElementById('confetti');box.innerHTML='';for(let i=0;i<110;i++){const p=document.createElement('i');p.className='piece';p.style.left=Math.random()*100+'%';p.style.setProperty('--dx',(-120+Math.random()*240)+'px');p.style.animationDelay=(Math.random()*.55)+'s';p.style.animationDuration=(1.8+Math.random()*1.2)+'s';box.appendChild(p)}setTimeout(()=>box.innerHTML='',3300)}function completeLevel(){confetti();const data=LEVEL===6?'Ciclo científico completo y enviado.':records.map(x=>x.label+' = '+x.value).join(' · ');document.getElementById('success-data').textContent=data;try{localStorage.setItem('apulab.level'+LEVEL+'.complete','1');localStorage.setItem('apulab.level'+LEVEL+'.data',data)}catch{}document.getElementById('success-overlay').classList.add('visible');updateGuideIfOpen()}
${level===6?"document.getElementById('continue-btn').onclick=()=>{try{parent.postMessage({type:'apulab-level-complete',level:6,nextLevel:7},location.origin)}catch{}};":"document.getElementById('finish-btn').onclick=()=>{document.getElementById('success-overlay').classList.remove('visible');showStatus('MISIÓN 01 COMPLETADA · DATOS GUARDADOS',3200)};"}
function dispose(){if(disposed)return;disposed=true;for(const id of [...rafIds])nativeCAF(id);rafIds.clear();try{renderer.setAnimationLoop(null);renderer.dispose();renderer.forceContextLoss?.()}catch{}}window.addEventListener('pagehide',dispose,{once:true});window.addEventListener('beforeunload',dispose,{once:true});window.addEventListener('message',e=>{if(e.data&&e.data.type==='apulab-dispose')dispose()});
try{parent.postMessage({type:'apulab-level-ready',level:LEVEL},location.origin)}catch{}
</script>
</body></html>`;
}

const manifestPath = resolve(OUT_DIR, 'manifest.json');
const manifest = JSON.parse(await readFile(manifestPath, 'utf8'));
for (const [key, cfg] of Object.entries(configs)) {
  const level = Number(key);
  const html = levelHtml(level, cfg);
  if (!html.includes(`${level} / 7`) || !html.includes('GUÍA · 3 PASOS') || !html.includes('REPETIR × N')) throw new Error(`mission01_level${level}_contract_failed`);
  await writeFile(resolve(OUT_DIR, `level${level}.html`), html, 'utf8');
  const entry = { level, sha256: hash(html), bytes: Buffer.byteLength(html, 'utf8') };
  manifest.levels = (manifest.levels || []).filter((x) => Number(x.level) !== level);
  manifest.levels.push(entry);
  console.info(`[mission01] Nivel ${level}/7 integrado · ${entry.bytes} bytes · ${entry.sha256}`);
}
manifest.totalLevels = 7;
manifest.availableLevels = [1,2,3,4,5,6,7];
manifest.unavailableLevels = [];
manifest.levels.sort((a,b)=>a.level-b.level);
manifest.migration = {
  ...(manifest.migration || {}),
  integratedDesignedLevels: { 6: 'mision-cientifica-con-bucle', 7: 'sensores-y-bucles' },
  note: 'Niveles 6 y 7 integrados desde el diseño pedagógico oficial; no existían archivos legacy 7/8 en el historial del repositorio.'
};
await writeFile(manifestPath, JSON.stringify(manifest,null,2)+'\n','utf8');
console.info('[mission01] Niveles 6–7 integrados · secuencia activa 1–7 completa');
